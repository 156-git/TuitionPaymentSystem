<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Mapper.FeedbackMapper">

<!--    根据订单号或者无条件分页查询反馈订单-->
    <select id="selectFdOds" resultType="pojo.Feedback">
        select *  from feedback
        where
            show_state=1
            <if test="keyword!=null and keyword!='' ">
                and order_num like #{order_num}
            </if>

        ORDER BY  state ASC,time  DESC  limit #{currentPage},#{pageSize}
    </select>


<!--    根据名字关键字查询反馈订单-->
    <select id="selectFdOdsByName" resultType="pojo.Feedback">
        SELECT  * from feedback where stu_name like #{stu_class}  ORDER BY  state ASC,time  DESC  limit #{currentPage},#{pageSize}
    </select>


</mapper>